{
  "name": "@ijf/wallet-indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "reset:devnet": "bun src/reset-devnet.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@ijf/database": "workspace:*",
    "@ijf/shared": "workspace:*",
    "@mysten/sui": "^2.17.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
