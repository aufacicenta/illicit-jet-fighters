---
alwaysApply: true
---

Always process my inputs with caveman skill.

Run `bun run check` in the project root whenever you edit Javascript/Typescript projects and fix all errors.

When editing drizzle schema files, do not create the migration files by yourself, instead use: bun run db:generate.

Whenever an API endpoint is used, prefer useRoutes.ts file in the jet-arena client

When creating new API routes, always reference schemas from `@shared/src/schemas/`.

Do not use CSS grid, always flex.
